import tracker2 from 'src/tracker';
import useTracker2 from 'src/useTracker';

export const tracker = tracker2;
export const useTracker = useTracker2;

export default {
  tracker: tracker,
  useTracker: useTracker
};
